import * as __React from 'react';
import * as __ReactDOM from 'react-dom';
import * as __ReactRedux from 'react-redux';

declare global {
    const React: typeof __React;
    const ReactDOM: typeof __ReactDOM;
    const ReactRedux: typeof __ReactRedux;
}